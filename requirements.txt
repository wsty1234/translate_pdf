# 学术PDF阅读器依赖
pdf2image>=1.16.3
Pillow>=10.0.0
requests>=2.31.0
python-dotenv>=1.0.0

# 可选：如果使用本地API或OpenAI官方库
# openai>=1.0.0

# 系统依赖（需要单独安装）：
# macOS: brew install poppler
# Ubuntu/Debian: apt-get install poppler-utils
# Windows: 下载poppler并添加到PATH
